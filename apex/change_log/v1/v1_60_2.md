# Apex Habitat 1.60.2

Fixes the four self-service portals showing their startup script as visible text instead of running it.

- The four self-service portals (driver, masar, safety, fleet) now load correctly instead of showing raw text.
- Theme and branding apply again on every portal page.
- Nothing changes retroactively; existing data is unaffected.
