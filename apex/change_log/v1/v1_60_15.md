# Apex Habitat 1.60.15

Security and reliability improvements in trips and routes.

- A driver can no longer overwrite another driver's trip start record.
- Route stops are tidier: each stop needs a name, and a route cannot be saved with blank stops.
- Nothing changes retroactively; existing trips and routes are unaffected.
