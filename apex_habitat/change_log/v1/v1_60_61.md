# Apex Habitat 1.60.61

The worker's Home next-ride card now shows the live 'arriving in N min' ETA from the driver's GPS position, replacing the plain scheduled time when the driver is on the way

- Home's next-ride card displays the server-computed live ETA in minutes once the driver is en route and sharing position
- Falls back to the truthful en-route state (no fabricated ETA) until a live position exists
- The 'arriving in' label is localized across all five worker languages
