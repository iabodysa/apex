# Apex Habitat 1.60.17

Frontend shared modules + Work Shift DocType + fetch chains

- Extract call() and makeCache() to frontend_shared; fleet/safety/driver/worker portals import from shared module (T-647, T-648)
- Add Work Shift, Work Shift Day, and Route Assignment DocTypes with fetch_from chain to Dispatch Trip and Trip Start Log (P-047)
- Add route_assignment and shift_name fetch fields to Dispatch Trip; shift_name propagates to Trip Start Log
