# Apex Habitat 1.55.12

Fix a fresh-install crash, driver/safety/fleet portal fixes, a Masar worker boarding-confirm, and a routed-payment concurrency test.

- Fix: worker-housing Item Groups now seed via a NestedSet-safe migration patch instead of a fixture, which broke a fresh-site install.
- Masar: a worker boarding-confirm ('I'm at the pickup') action that logs a Trip Boarding Event.
- Driver portal: a 5-tab bottom bar with the Route and Vehicle screens reachable.
- Safety portal: correct RTL icon mirroring (the flip is scoped to icons, not all content).
- Fleet Operations workspace: all eight number cards render (label/name match fixed).
- Tests: a routed-payment concurrency test; boarding endpoints covered by the permission-recheck allowlist.
