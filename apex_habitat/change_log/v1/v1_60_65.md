# Apex Habitat 1.60.65

Loading placeholders on five operator Desk pages are visible again.

- Fixed the skeleton loading placeholders on the Front Desk, Safety Map, Transfer Board, Operations Control, and Dispatch Board pages, which were rendering invisible because they used a style class that does not exist in the framework.
- Each placeholder now uses the native Desk loading tint, so users see a clear loading state instead of a blank area.
- No behavior change; full test suite green (1601 tests).
