# Apex Habitat 1.60.2

Fixes the four self-service portals showing their startup script as visible text instead of running it.

- Fixed all four self-service portals (driver, masar, safety, fleet) rendering their inline bootstrap script as visible text — a code comment contained a literal closing-script tag that ended the script early, so csrf-token, theme and branding were never set; removed the inline-script comments.
- Consolidated driver resolution into one shared helper.
- Reliability and code-hygiene improvements across the portal host pages.
