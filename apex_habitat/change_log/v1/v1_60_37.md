# Apex Habitat 1.60.37

Correction wave 2 — approval workflows now install and enforce, scope-tested reads, print guard.

- Approval workflows now actually install and enforce separation of duties on their documents (previously shipped as inert definitions that were never seeded).
- Report and custody record reads are now scope-tested so each user only sees the rows their permissions allow.
- A print guard now handles a deleted building link so a label still renders instead of erroring.
