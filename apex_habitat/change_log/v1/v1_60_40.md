# Apex Habitat 1.60.40

Correction wave 5: desk pages drop bespoke CSS for native styling, deterministic workspace rendering, and plain-number arrivals counters

- Desk pages now use native Frappe styling (custody kiosk, arrivals desk, fuel approval console) instead of bespoke custom CSS
- Workspace shortcuts now render reliably and link order is deterministic
- Arrivals desk summary counters display plain numbers instead of raw markup
