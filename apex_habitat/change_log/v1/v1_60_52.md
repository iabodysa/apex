# Apex Habitat 1.60.52

Robustness pass: malformed-JSON guards, query indexes, notification-config hardening, and a test-isolation fix.

- Malformed JSON in count and checklist submissions now returns a clean validation error instead of a server error.
- Added search and composite database indexes to speed up ledger, alert, and fleet queries.
- Hardened messaging and push notification config-read failure paths so a transient error no longer aborts the job.
- Fixed a test-isolation leak so workflow test data no longer persists across the suite.
