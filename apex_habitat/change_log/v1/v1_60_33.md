# Apex Habitat 1.60.33

Hotfix: completes an incomplete schema rename that had broken custody and transport queries.

- Fix runtime database errors in custody balance, kiosk, weekly digest, driver-portal stop ordering and the top-workers chart caused by an incomplete column rename.
- Repoint all stock-ledger reads to the signed-quantity column and route-stop ordering to the row index.
- Restore the permissions baseline check by auditing the workspace-rename migration call sites.
