# Apex Habitat 1.55.17

Arabic translation coverage, a one-call driver home summary, a model cleanup, a wider safety scan, and building address display reconciliation.

- Localization: Arabic coverage completed for a batch of recently added interface strings.
- Driver portal: a single home-summary endpoint now returns attendance, the next trip, licence countdown, vehicle-bound state, and any open clearance in one call, so the home screen paints in one round-trip.
- Housing: removed an unused worker field that had no intake behind it.
- Safety: the daily compliance scan now also flags active buildings that have no recorded safety round in the recent window, not just overdue tasks.
- Housing: a building's own recorded address now shows on its form even when no site is set, instead of being shadowed by the site address.
