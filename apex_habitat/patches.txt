[pre_model_sync]
# Patch lifecycle policy (audit 2026-06-03):
#   PERMANENT = recurring/idempotent seeds; must stay so fresh installs replay them.
#   ONE-TIME  = renames/cleanups/backfills; PRUNE (remove module + this line) once
#              every deployed site has run it (tracked in tabPatch Log).
#   Backfills are no-ops on fresh installs — never rely on them to seed fresh DBs.
#
#   2026-06-03 prune (no prod sites yet → gate vacuously met): removed 20 spent
#   one-time / no-op patches — renames, backfills, workspace merges, cache/filter
#   fixes, and the M-10/M-11-superseded kanban/assignment/email/salis-artifact
#   no-ops (those records are now provisioned by the data-driven loader
#   apex_core/setup/seed.seed_all, wired into after_install + after_migrate).
#   HELD on purpose: add_city_doctype (the ONLY City seed path — no data/ JSON,
#   no other inserter), rename_habitat_city_to_city (date-gated, remove on/after
#   2026-06-24), v1_x.ensure_web_form_route (recent GitHub #97 route safeguard),
#   seed_salis_operations_roles (a guarded no-op but still imported by hooks +
#   salis/setup, so its module must stay). PERMANENT seeders kept below.
apex_habitat.patches.v0_6.add_city_doctype
apex_habitat.patches.v0_7.rename_habitat_city_to_city
apex_habitat.patches.v0_7.add_bed_assignment_index

# v0.8 — data completeness (PERMANENT, idempotent seeder)
apex_habitat.patches.v0_8.seed_missing_material_templates

# v0.8.4 — automation & communication (PERMANENT seeders + idempotent navbar)
apex_habitat.patches.v0_8.seed_auto_email_reports
apex_habitat.patches.v0_8.add_navbar_help_links

# v0.8.6 — navbar route fix (Single, idempotent) + dashboards (PERMANENT seeders)
apex_habitat.patches.v0_8.fix_navbar_command_center_route
apex_habitat.patches.v0_9.seed_habitat_dashboard
apex_habitat.patches.v0_9.seed_role_dashboards

# v1.0 — Salis fleet module (PERMANENT, idempotent, existence-guarded seeders).
# seed_salis_operations_roles is a guarded no-op kept because hooks + salis/setup
# import it; the others seed DISTINCT records and fresh installs must replay them.
apex_habitat.patches.v1_0.seed_salis_roles
apex_habitat.patches.v1_0.seed_salis_authority_roles
apex_habitat.patches.v1_0.seed_salis_operations_roles
apex_habitat.patches.v1_0.seed_salis_settings
apex_habitat.patches.v1_0.seed_salis_portal_theme

# v1.x — Salis communications + native Workflow spine (PERMANENT, idempotent).
# Frappe does not auto-import a Workflow from a module folder, so seed_salis_workflows
# is the install path; both also run from after_install/after_migrate.
apex_habitat.patches.v1_x.seed_salis_communications
apex_habitat.patches.v1_x.seed_salis_workflows

# v1.55.x — seed the Accommodation Item Groups (PERMANENT, idempotent, NestedSet-safe).
# Item Group is a NestedSet; a fixture import crashed fresh-site install on the NULL
# lft/rgt root. Runs before sync_fixtures, so the groups exist when item.json loads.
apex_habitat.patches.v1_x.seed_accommodation_item_groups

# v1.50.8 — workspace canonical rename (ONE-TIME). Removes the four stale workspace
# records left behind when Habitat/Accommodation/Facilities/Apex Core were renamed to
# Habitat Hub/Housing/Safety/Launchpad. PRUNE once every deployed site has run it.
apex_habitat.patches.v1_x.rename_habitat_workspaces

# v1.52.2 — Accommodation Site native Address backfill (ONE-TIME). Creates a Frappe
# Address record for each site that has city/district data and no linked Address yet.
# Mirrors the building backfill pattern. PRUNE once every deployed site has run it.
apex_habitat.patches.v1_x.backfill_accommodation_site_native_address

[post_model_sync]
# v1.x — TEMPORARY safeguard (GitHub #97): ensure the Web Form keeps its `route` on
# an upgrade (the v15-removed make_route_to_web_form helper is never needed).
# Idempotent, only-if-missing (never clobbers a custom route), no-op on fresh installs.
# PRUNE once every deployed site is confirmed v15-correct (tracked in tabPatch Log).
apex_habitat.patches.v1_x.ensure_web_form_route

# v1.x — NATIVE-FIRST dashboards (ONE-TIME). The Habitat + Salis Dashboard parents now
# ship as is_standard=1 module JSON (habitat/habitat_dashboard/, salis/salis_dashboard/),
# imported by sync_dashboards. This deletes the legacy is_standard=0 Dashboard rows the
# retired runtime seeders created so the SAME-migrate sync_dashboards() step (runs AFTER
# post_model_sync patches) re-imports them as is_standard=1. GUARD: deletes a Dashboard
# only if its is_standard==0; idempotent; no-op on fresh installs. PRUNE once every
# deployed site has run it (tracked in tabPatch Log).
apex_habitat.patches.v1_x.drop_legacy_is_standard0_dashboards

# v1.52.x — backfill total_rooms / total_floors on existing buildings (ONE-TIME). The
# controller now derives both from the generated rooms; rows saved earlier showed stale 0.
# Idempotent, no-op on fresh installs. PRUNE once every deployed site has run it.
apex_habitat.patches.v1_x.backfill_building_room_floor_counts

# v1.52.x — relocate app-wide finance settings (ONE-TIME). default_payment_method and
# enable_gl_posting moved from Habitat Settings to the new app-wide Apex Settings single
# (they serve both Habitat and Salis). Copies any value a site already set on Habitat
# Settings into Apex Settings, then deletes the Habitat Settings orphan rows. Idempotent,
# no-op on fresh installs. PRUNE once every deployed site has run it.
apex_habitat.patches.v1_x.move_finance_settings_to_apex_settings

# v1.x — Salis transport-type service_line refactor (ONE-TIME). Transport Request
# service_line is now the transport type (Site Transport / Inter-City Relocation /
# Administrative Trip) instead of the Workers/Representatives division. Maps existing
# rows: 'Workers' -> 'Inter-City Relocation' when its request_type is an inter-city
# relocation, else 'Site Transport'; 'Representatives' -> 'Administrative Trip' (gov
# item 62 — they were submitted as transport requests, so they stay مسار). Idempotent,
# no-op on fresh installs / re-run. PRUNE once every deployed site has run it.
apex_habitat.patches.v1_x.refactor_transport_request_service_line
# Rename the worker-transport Workspace 'Movement' -> 'Masar' so /app/masar
# resolves (owner-reported 404). Idempotent; no-op on fresh installs / re-run.
apex_habitat.patches.v1_x.rename_movement_workspace_to_masar

# v1.54.x — backfill Accommodation Building.building_address (ONE-TIME, T-269). For each
# building whose building_address is empty but which has a legacy default Address linked
# via Dynamic Link, set building_address = get_default_address(...). Never clobbers a set
# value; skips buildings with no resolvable Address. Idempotent, no-op on fresh installs.
# PRUNE once every deployed site has run it (tracked in tabPatch Log).
apex_habitat.patches.v1_x.backfill_building_address_from_default_address

# v1.55.0 — retire the superseded Safety Checklist desk Page (T-290, ONE-TIME). The
# /safety Vue portal (1.54.29) replaced the desk Page; its standard files were removed,
# but the Page and its 'Record a Safety Inspection' onboarding step persist as DB orphans
# on existing sites and the page 500s on click (load_assets on a missing dir). Deletes
# both orphans. Idempotent, no-op on fresh installs / re-run. PRUNE once every deployed
# site has run it (tracked in tabPatch Log).
apex_habitat.patches.v1_x.retire_safety_checklist_desk_page

# v1.55.x — privacy: strip the confidential client / H&S-accreditation-vendor names from
# already-seeded Safety Task Catalog rows (T-291, ONE-TIME). The seed file was genericized
# to client-neutral wording; the create-only loader never rewrites existing rows, so this
# re-applies the corrected task_title/instructions from the clean JSON to any
# row still carrying a confidential token. Idempotent, no-op on fresh installs / re-run.
# PRUNE once every deployed site has run it (tracked in tabPatch Log).
apex_habitat.patches.v1_x.genericize_safety_task_catalog_client

# v1.55.x — enable the Custody Damage Assessment Created notification on existing sites
# (ONE-TIME). Notification.enabled is in import_file.ignore_values, so migrate preserves
# the stale DB value and never applies the JSON enabled flip; flip enabled +
# send_system_notification on the live row. Idempotent, no-op on fresh installs / re-run.
# PRUNE once every deployed site has run it (tracked in tabPatch Log).
apex_habitat.patches.v1_x.enable_custody_damage_assessment_notification

# v1.55.x — backfill Accommodation Assignment.responsible_facility_supervisor (ONE-TIME,
# T-569). The field fetches from building.responsible_facility_supervisor on save, so rows
# saved before the field existed stay empty and the Expected Checkout reminder never reaches
# the building supervisor for them. Copies the building's supervisor into each empty row.
# Never clobbers a set value; skips buildings with no supervisor. Idempotent, no-op on
# fresh installs / re-run. PRUNE once every deployed site has run it (tracked in tabPatch Log).
apex_habitat.patches.v1_x.backfill_assignment_facility_supervisor

# v1.55.x — enable the Vehicle Theft Reported + Workshop Overstay notifications on existing
# sites (ONE-TIME). Notification.enabled is in import_file.ignore_values, so migrate
# preserves the stale DB value and never applies the JSON enabled flip; flip enabled +
# send_system_notification on the live rows. Idempotent, no-op on fresh installs / re-run.
# PRUNE once every deployed site has run it (tracked in tabPatch Log).
apex_habitat.patches.v1_x.enable_salis_theft_overstay_notifications

# v1.x — enable the driver-facing Blocked Driver Clearance + Vehicle Compliance Expiring
# Soon notifications on existing sites (ONE-TIME, T-562). Notification.enabled is in
# import_file.ignore_values, so migrate preserves the stale DB value and never applies the
# JSON enabled flip; flip enabled + send_system_notification on the live rows. Idempotent,
# no-op on fresh installs / re-run. PRUNE once every deployed site has run it (tracked in
# tabPatch Log).
apex_habitat.patches.v1_x.enable_salis_driver_notifications

# v1.x — enable the Custody Return Overdue notification on existing sites (ONE-TIME).
# The recipient was repointed to issued_to_user, but the record shipped enabled:0 and
# Notification.enabled is in import_file.ignore_values, so migrate preserves the stale
# DB value and never applies the JSON enabled flip; flip enabled +
# send_system_notification on the live row. Idempotent, no-op on fresh installs / re-run.
# PRUNE once every deployed site has run it (tracked in tabPatch Log).
apex_habitat.patches.v1_x.enable_custody_return_overdue_notification

# v1.x — seed a small demo worker-movement scenario (developer_mode only) so Masar +
# the driver portal are not empty on a first look. Gated behind developer_mode +
# required DocTypes; idempotent get-or-create on stable demo keys; generic placeholders
# (no real PII); never aborts install/migrate. PERMANENT — fresh demo sites replay it.
apex_habitat.patches.v1_x.seed_masar_demo_movement

# v1.x — seed two ready-to-use demo logins (supervisor + employee personas) so the
# owner can test each role-scoped surface; reuses the seed_masar_demo_movement demo
# estate so the Project/trip exist first. Gated behind developer_mode + required
# DocTypes; idempotent get-or-create on stable keys; never aborts install/migrate.
apex_habitat.patches.v1_x.seed_demo_role_logins

# v1.x — stamp last_known_versions[apex_habitat] for users already past the changelog
# popup, so the version-feed migration to a tracked seen-version does not re-show old
# release notes. Touches only the changelog-seen value; preserves other apps' entries.
# Idempotent; no-op on fresh installs / re-run.
apex_habitat.patches.v1_x.stamp_changelog_seen_version

# v1.x — reconcile Custody Issue.issued_to_user to valid User names (ONE-TIME). The
# field changed from a free Data field to Link -> User; runs AFTER model sync so the
# Link constraint exists. Keeps already-valid User names, remaps an email match to its
# User name, blanks truly unresolvable values (an invalid Link value would block later
# saves). Idempotent, no-op on fresh installs / re-run. PRUNE once every deployed site
# has run it (tracked in tabPatch Log).
apex_habitat.patches.v1_x.map_custody_issue_user_to_link

# v1.60.8 — drop the bundled System Manager role from the existing Habitat
# Accommodation Manager Role Profile (T-668, ONE-TIME). The seed fix only covers
# fresh installs; this removes the privilege-escalation row on already-provisioned
# sites. Idempotent, no-op on fresh installs / re-run. PRUNE once every deployed
# site has run it (tracked in tabPatch Log).
apex_habitat.patches.v1_x.drop_sysmgr_from_accommodation_manager_profile

# v1.x — adopt native Frappe Address for Rental Office (T-532, ONE-TIME). Rental Office
# now wires the native Address (Address DocType + Dynamic Link, ERPNext pattern) via an
# address_html field; this mirrors each office's legacy inline `city` value into a native
# Address record. Runs AFTER model sync so the address_html field + Dynamic Link target
# exist. Skips offices already linked or with no city; never deletes the inline value.
# Idempotent, no-op on fresh installs / re-run. PRUNE once every deployed site has run it.
apex_habitat.patches.v1_x.backfill_rental_office_native_address
